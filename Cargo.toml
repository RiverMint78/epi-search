[package]
name = "epi-search"
version = "0.2.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
mimalloc = "0.1"
rayon = "1.12"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
